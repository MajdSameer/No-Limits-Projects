{
  "name": "no-limits-projects",
  "private": true,
  "description": "Monorepo for No Limits Removalists web tools — every project plugs into this foundation.",
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "dev": "pnpm --filter @nlr/template dev",
    "lint": "eslint . && pnpm -r run lint",
    "typecheck": "pnpm -r run typecheck",
    "build": "pnpm -r run build",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run build",
    "new-app": "node scripts/new-app.mjs"
  },
  "devDependencies": {
    "@nlr/config": "workspace:*",
    "eslint": "^10.4.1"
  }
}
